import {useState} from "react";type Props={loading:boolean;onSearch:(q:string)=>void};export function SearchBar({loading,onSearch}:Props){const[q,setQ]=useState("");const go=()=>{const v=q.trim();if(v)onSearch(v)};return <section className="search-panel"><span className="eyebrow">Pesquisa ao vivo</span><h1>Qual produto você quer analisar?</h1><p>Consulte anúncios atuais e receba uma leitura inicial de preço e concorrência.</p><div className="search-row"><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==="Enter"&&go()} placeholder="Ex.: organizador de cabos"/><button onClick={go} disabled={loading}>{loading?"Pesquisando...":"Pesquisar"}</button></div></section>}